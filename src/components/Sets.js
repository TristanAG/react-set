import React from 'react'


  //do a for loop, and then do a map of cards within that for loop for each set
  //do a for loop, and then do a map of cards within that for loop for each set
  //do a for loop, and then do a map of cards within that for loop for each set
  //do a for loop, and then do a map of cards within that for loop for each set
  //do a for loop, and then do a map of cards within that for loop for each set
  //do a for loop, and then do a map of cards within that for loop for each set
  //do a for loop, and then do a map of cards within that for loop for each set
  //do a for loop, and then do a map of cards within that for loop for each set


const Sets = ({sets}) => (
  <div>
    {
      sets.length
    }
  </div>
)

export default Sets
